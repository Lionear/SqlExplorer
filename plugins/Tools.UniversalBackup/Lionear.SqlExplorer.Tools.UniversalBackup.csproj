<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Lionear.SqlExplorer.Tools.UniversalBackup</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Contract only. No driver/library dependency — this tool talks to the connection purely through
         the host's IDbProvider, and uses BCL crypto/compression. Private=false keeps Sdk.dll out of the
         plugin folder so the host's copy is used across the ALC boundary. -->
    <ProjectReference Include="..\..\src\Sdk\Lionear.SqlExplorer.Sdk.csproj" Private="false" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="plugin.json" CopyToOutputDirectory="PreserveNewest" />
    <EmbeddedResource Include="icon.png" LogicalName="icon.png" Condition="Exists('icon.png')" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Lionear.SqlExplorer.Sdk.Connections" />
    <Using Include="Lionear.SqlExplorer.Sdk.Schema" />
    <Using Include="Lionear.SqlExplorer.Sdk.Query" />
    <Using Include="Lionear.SqlExplorer.Sdk.Ddl" />
    <Using Include="Lionear.SqlExplorer.Sdk.Branding" />
    <Using Include="Lionear.SqlExplorer.Sdk.Tools" />
  </ItemGroup>

</Project>
